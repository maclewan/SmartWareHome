services:
  django:
    build: .
    restart: always
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:7983"
    volumes:
      - .:/backend_app
    ports:
      - "7983:7983"
